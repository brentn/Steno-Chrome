Unit Tests:


Listener for cursor moved:
- onSurroundingTextChanged

Load multiple dictionaries

Better dictionary choosing gui

Formatter
- remove space at start
- remove space at end
- capitalize
- lowercase


 DONE
======
Translator:
  if (undo):
    if (no preview):
      get preview from history
    if (preview):
      undo preview:
    else (if no preview):
      **backspace
  else (if not undo):
    if (preview+translation found):
      append translation to preview
      if (not ambiguous):
        commit and clear preview
    else (preview+translation not found):
      if (preview not empty):
        commit and clear preview
        recurse with new translation only
      else (preview empty):
        add raw stroke
        
Options:
- dictionary file(s)
- input selector
- output selector
- space placement
- undo size